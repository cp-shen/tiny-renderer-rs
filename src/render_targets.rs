use crate::objects::*;

trait RenderTarget {
    fn render(scene: Scene);
}

struct ImageRenderTarget {
    //TODO
}

impl RenderTarget for ImageRenderTarget {
    fn render(scene: Scene) {
        //TODO
    }
}
